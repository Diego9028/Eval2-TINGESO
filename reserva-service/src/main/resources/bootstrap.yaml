spring:
  application:
    name: reserva-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/bd_reservas
    username: postgres
    password: grupo4
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: kartingmicros@gmail.com
    password: kjyf zypl jeyh pzjy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
